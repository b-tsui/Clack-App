// this code might have to go in index.js to work in conjunction with socket.io
// this only works when input element is changed to a div in the main.pug with an attribute of contenteditable="true"

// function format(command, value) {
//     document.execCommand(command, false, value);
// };


// const bold = document.getElementById("bold");

// bold.addEventListener("click", event => {
//     format("bold");
// });